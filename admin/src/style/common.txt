:root {
  /* colors */
  --surface: #f9fafb;
  --surface-alt: #ffffff;
  --text: #0f172a;
  --text-subtle: #525252;
  --border: #d4d4d4;

  /* Neutrals */
  --brand-gray-400: #727272; /* body text, secondary UI */
  --brand-gray-800: #222222; /* headings, strong text */

  /* Blue — Primary (링크/주버튼) */
  --brand-blue-400: #60a5fa; /* hover bg / subtle accents */
  --brand-blue-500: #3b82f6; /* primary */
  --brand-blue-600: #2563eb; /* primary text/icon on light */
  --brand-blue-800: #1e40af; /* active/pressed, deep accents */

  /* Green — Success */
  --brand-green-400: #22c55e;
  --brand-green-600: #16a34a;
  --brand-green-700: #15803d;
  --brand-green-800: #166534;

  /* Red — Danger */
  --brand-red-400: #ff5151;
  --brand-red-600: #dc2626;
  --brand-red-700: #b91c1c;
  --brand-red-800: #720f0f;

  /* Orange — Warning / 강조 */
  --brand-orange-400: #fb923c;
  --brand-orange-500: #f97316;
  --brand-orange-600: #ea580c;
  --brand-orange-800: #9a3412;

  /* status */
  --status-approved: #1ea97c;
  --status-pending: #f1a40f;
  --status-rejected: #e65656;
  --status-info: #546de5;

  --status-approved-hover: #15875e;
  --status-pending-hover: #d18b0c;
  --status-rejected-hover: #c23b3b;
  --status-info-hover: #435bbd;

  /* focus ring */
  --focus-ring: #3b82f6;
  --focus-ring-offset: 0.125rem;
  --focus-ring-size: 0.1875rem;

  /* radius & spacing */
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;

  /* shadows */
  --elevation-1: 0.375rem 0.375rem 0.75rem rgba(0, 0, 0, 0.06),
    -0.375rem -0.375rem 0.75rem rgba(255, 255, 255, 0.9);

  /* table head (new) */
  --table-head-bg: #f4f6fa; /* soft light blue, 어울리는 중립 계열 */
  --table-head-text: #171717;

  --newmorphism-shadow-inset: inset 0.2rem 0.2rem 0.5rem var(--border),
    inset -0.2rem -0.2rem 0.5rem var(--surface-alt);
  --newmorphism-shadow: 0.2rem 0.2rem 0.5rem var(--border),
    -0.2rem -0.2rem 0.5rem var(--surface-alt);
}

/* Optional page themes */
[data-theme='vendor'] {
  --surface: #ffffff;
  --elevation-1: none;
}
[data-theme='user'] {
  --surface: #ffffff;
  --elevation-1: 0 0 0 0.1875rem #000;
}

/* ---------- Utilities ---------- */
.list-reset {
  list-style: none;
  margin: 0;
  padding: 0;
}
.grid {
  display: grid;
  gap: var(--space-4);
}
.grid--cols-4 {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.sr-only {
  position: absolute;
  width: 0.0625rem;
  height: 0.0625rem;
  padding: 0;
  margin: -0.0625rem;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.icon {
  line-height: 1;
  font-size: 1.5rem;
}

/* Focus utilities (opt-in) */
.u-focus-reset:focus {
  outline: none;
  box-shadow: none;
}
.u-focus-ring:focus-visible {
  outline-offset: var(--focus-ring-offset);
}
@media (forced-colors: active) {
  .u-focus-ring:focus-visible {
    outline: 0.125rem solid CanvasText;
  }
}

/* Search input decoration clean (opt-in for type="search") */
.u-search-clean[type='search']::-webkit-search-cancel-button,
.u-search-clean[type='search']::-webkit-search-decoration {
  -webkit-appearance: none;
  appearance: none;
}

/* Touch highlight remove (opt-in) */
.u-no-tap-highlight {
  -webkit-tap-highlight-color: transparent;
}

/* ---------- Layout ---------- */
.container {
  width: 100%;
  display: flex;
  align-items: flex-start;
  background-color: #f4f6fa;
}

.sidebar-container {
  flex: 0 0 15%;
}

/* ---------- Badge ---------- */
.badge {
  display: inline-block;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
  font-weight: bold;
  color: var(--text);
  text-align: center;
  white-space: nowrap;
}

.badge--info {
  background-color: var(--status-info);
  color: var(--surface-alt);
}

.badge--pending {
  background-color: var(--status-pending);
  color: var(--surface-alt);
}

.badge--approve {
  background-color: var(--status-approved);
  color: var(--surface-alt);
}

.badge--rejected {
  background-color: var(--status-rejected);
  color: var(--surface-alt);
}

/* ---------- Header (block: header) ---------- */
.header {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4) calc(var(--space-4) * 2);
  border-bottom: 0.1rem solid #ccc;
  background-color: var(--surface-alt);
  color: var(--text);
}

.header__logo {
  font-family: '학교안심 알림장', sans-serif;
  text-decoration: none;
  color: inherit;
}
.header__logo .header__logo-link {
  font-size: 1.3rem;
}

.header__icon {
  width: 100%;
}
.header__icon i {
  color: blue;
}

.header__path {
  width: 50%;
  color: var(--text-subtle);
}
.header__current-path {
  font-weight: bold;
  font-size: 1.1rem;
  color: #000;
}

.header__nav {
  width: 10%;
}
.header__menu {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-around;
  gap: 1rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

/* ---------- Sidebar (block: sidebar) ---------- */
.sidebar {
  color: var(--text);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: calc(var(--space-4) * 2) var(--space-4);
  font-size: 0.7rem;
}

.sidebar__menu {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

.sidebar__item {
  width: 100%;
  text-align: left;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-direction: column;
}

.sidebar__main {
  width: 100%;
  background-color: var(--surface-alt);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-weight: bold;
  padding: 0.5rem 1rem;
}

.sidebar__left {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.sidebar__submenu-wrapper {
  width: 100%;
  height: 0;
  opacity: 0;
  overflow: hidden;
  transition: height 0.3s ease, opacity 0.3s ease;
  will-change: height, opacity;
  padding: 0.5rem 0.5rem 0;
}

/* opened state (opacity handled by class; height is controlled in JS) */
.sidebar__submenu-wrapper.is-open {
  opacity: 1;
}

.sidebar__submenu {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
}

.sidebar__submenu-item {
  width: 100%;
  padding: 0.3rem 0.5rem;
  border-radius: var(--radius-sm);
  transition: all 0.3s ease-in-out;
}
.sidebar__submenu-item:hover {
  background-color: var(--surface-alt);
  cursor: pointer;
  border-left: 0.2rem solid blue;
}

/* chevron rotation (respect aria-expanded) */
.sidebar__toggle-btn[aria-expanded='true'] .fa-chevron-down {
  transform: rotate(180deg);
  transition: transform 0.2s ease;
}

/* motion reduce */
@media (prefers-reduced-motion: reduce) {
  .sidebar__submenu-wrapper {
    transition: none;
  }
  .sidebar__toggle-btn .fa-chevron-down {
    transition: none;
  }
}

/* ===== Data Table (BEM) ===== */
.content-wrapper {
  display: flex;
  flex-direction: column;
  gap: var(--space-4, 1rem);
  background-color: var(--surface-alt);
  border-radius: var(--radius-md, 0.5rem);
  padding: var(--space-4, 1rem) calc(var(--space-4, 1rem) * 2);
  box-shadow: var(--newmorphism-shadow);
}

.data-table-wrapper {
  overflow: auto; /* sticky header/가로 스크롤 지원 */
}

.data-table__header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: var(--space-4, 1rem);
}

.data-table__search-wrapper {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  flex: 0.5;
}

.data-table__search-input {
  flex: 1;
  padding: 0.5rem 1rem;
  border: 0.1rem solid var(--border, #d4d4d4);
  border-radius: var(--radius-md, 0.5rem);
  font-size: 1rem;
  background-color: #ffffff;
  color: var(--text, #171717);
}

.data-table__action-group {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

/* 기본 테이블 */
.data-table__table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  color: var(--text);
  min-width: 960px; /* 좁은 화면에서 스크롤 */
}
.data-table__head {
  background-color: var(--table-head-bg);
  color: var(--table-head-text);
}

/* 헤더 */
.data-table__head th {
  color: var(--text, #0f172a);
  text-align: center;
  font-weight: 600;
  padding: var(--space-3) var(--space-4);
  border-bottom: 0.1rem solid var(--border);
}

.data-table-body {
  background-color: var(--surface-alt);
}

/* 셀 공통 */
.data-table__row .data-table__cell,
.data-table__row th.data-table__cell {
  text-align: center;
  padding: var(--space-3) var(--space-4);
  border-bottom: 0.1rem solid var(--border);
  vertical-align: middle;
  text-overflow: ellipsis;
}

/* 선택 컬럼/체크박스 */
.data-table__cell--select {
  width: 2.5rem;
  text-align: center;
  padding-left: var(--space-2);
  padding-right: var(--space-2);
}
.data-table__checkbox {
  width: 1rem;
  height: 1rem;
}

/* 행 머리글(첫 컬럼) 고정 선택 시 사용 가능 */
.data-table__cell--row-header {
  position: sticky;
  left: 0;
  z-index: 1;
  background-color: var(--surface-alt);
  font-weight: 600;
}

/* 정렬/줄바꿈 유틸 모디파이어 */
.data-table__cell--right {
  text-align: right;
}
.data-table__cell--center {
  text-align: center;
}
.data-table__cell--nowrap {
  white-space: nowrap;
}
.data-table__cell--truncate {
  max-width: 24ch;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 빈 값 표시 */
.data-table__empty {
  color: var(--text-subtle);
}

/* hover/선택 상태 */
.data-table__body tr:hover td,
.data-table__body tr:hover th.data-table__cell {
  background-color: rgba(0, 0, 0, 0.02);
}
.data-table__row.is-selected td,
.data-table__row.is-selected th.data-table__cell {
  background-color: rgba(59, 130, 246, 0.08); /* brand-blue-500 투명도 */
}

/* 정렬 가능한 헤더(아이콘/화살표는 아이콘 폰트로 대체 가능) */
.data-table__sort {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
}
.data-table__sort[aria-sort='ascending']::after {
  content: '▲';
  font-size: 0.75em;
}
.data-table__sort[aria-sort='descending']::after {
  content: '▼';
  font-size: 0.75em;
}

/* ===== Pagination (BEM) ===== */
.pagination {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
  padding: var(--space-3) var(--space-4);
  background-color: var(--surface-alt);
  color: var(--text);
}

.pagination__status {
  font-size: 0.875rem;
  color: var(--text-subtle);
}

/* nav + list */
.pagination__nav {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.pagination__list {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

/* items & links */
.pagination__item {
  /* li */
  list-style: none;
}

.pagination__link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 2.25rem; /* 터치 타깃 확보 */
  height: 2.25rem;
  padding: 0 var(--space-3);
  border-radius: var(--radius-sm);
  background-color: var(--surface-alt);
  color: var(--text);
  text-decoration: none;
  line-height: 1;
  transition: box-shadow 0.15s ease, background-color 0.2s ease,
    border-color 0.2s ease, color 0.2s ease, transform 0.06s ease;
  box-shadow: var(--newmorphism-shadow-inset);
}

.pagination__link:hover {
  background-color: var(--surface);
}

.pagination__link:focus-visible {
  outline: var(--focus-ring-size) solid var(--focus-ring);
  outline-offset: var(--focus-ring-offset);
}

/* current page */
.pagination__link--current,
.pagination__link[aria-current='page'] {
  background-color: var(--brand-blue-600);
  color: var(--surface-alt);
  cursor: default;
  box-shadow: var(--newmorphism-shadow);
}

/* disabled */
.pagination__link[aria-disabled='true'] {
  opacity: 0.45;
  pointer-events: none; /* 키보드/마우스 비활성 */
}

.pagination__icon {
  font-size: 1rem;
  line-height: 1;
}

/* ellipsis */
.pagination__ellipsis {
  min-width: 2.25rem;
  height: 2.25rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--text-subtle);
}

/* 고대비 모드 보조 */
@media (forced-colors: active) {
  .pagination__link {
    border: 0.0625rem solid ButtonBorder;
    background-color: Canvas;
    color: CanvasText;
  }
  .pagination__link--current,
  .pagination__link[aria-current='page'] {
    border-color: Highlight;
    background-color: Highlight;
    color: HighlightText;
  }
}
